we don't use EUI anymore, and currently Axatin has hardcoded it to 4 UC, so we need to edit the lua and xml UI files to get back to 6
we also introduce code to find the improvements, which before needed dummies to show up in the UI
we use the particular orderpriority of 90 to make sure other civ-specific builds dont show up twice (like the hidden water polder)
PopulateUniques.lua is from (3a)